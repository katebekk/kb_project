{% extends 'base.html.twig' %}

{% block title %}GaleryUser{% endblock %}

{% block body %}
    <h1>GaleryUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ galery_user.id }}</td>
            </tr>
            <tr>
                <th>Nickname</th>
                <td>{{ galery_user.nickname }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ galery_user.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('galery_user_index') }}">back to list</a>

    <a href="{{ path('galery_user_edit', {'id': galery_user.id}) }}">edit</a>

    {{ include('galery_user/_delete_form.html.twig') }}
{% endblock %}
